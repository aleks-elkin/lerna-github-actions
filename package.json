{
  "name": "@aleks-elkin-test/lerna-repo",
  "version": "0.1.0",
  "description": "test to publish a Lerna monorepo with Github Actions",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-lerna-scopes": "^12.1.4",
    "@types/node": "^15.12.5",
    "husky": "^6.0.0",
    "lerna": "^4.0.0",
    "typescript": "^4.3.4"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run tsc",
    "publish": "npm run build && lerna publish --conventional-commits",
		"publish:github": "lerna publish from-git --no-git-tag-version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
